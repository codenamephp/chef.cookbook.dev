---
driver:
  name: vagrant

provisioner:
  name: chef_solo

verifier:
  name: inspec

platforms:
  - name: debian-9.3

suites:
  - name: default
    run_list:
      - recipe[codenamephp_dev::chrome]
      - recipe[codenamephp_dev::dnsmasq]
      - recipe[codenamephp_dev::openjdk8]
    verifier:
      inspec_tests:
        - test/smoke/default
    attributes:
